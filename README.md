The Analysis file is the "Control panel" for the overall program.
To work it, comment in the desired function to be run, with the desired particle, production method, rapidity, and (where relevant) lambda value
